#!/usr/bin/env bash
set -euo pipefail
source "$(cd "$(dirname "$0")" && pwd)/lib/common.sh"
run_unity "validate-learning" -executeMethod PequenoExplorador.Editor.BuildTools.BuildToolsCli.ValidateLearning
