#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "${SCRIPT_DIR}/lib/common.sh"
run_unity validate-localization -executeMethod PequenoExplorador.Editor.BuildTools.BuildToolsCli.ValidateLocalization
