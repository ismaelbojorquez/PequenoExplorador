#!/usr/bin/env bash

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "${SCRIPT_DIR}/lib/common.sh"

run_unity adopt-vs-d-a01 \
  -executeMethod PequenoExplorador.Editor.VerticalSliceContentAdoptionSetup.Apply
