#!/usr/bin/env bash
set -euo pipefail
source "$(cd "$(dirname "$0")" && pwd)/lib/common.sh"
run_unity "validate-customization" -executeMethod PequenoExplorador.Editor.BuildTools.BuildToolsCli.ValidateCustomization
