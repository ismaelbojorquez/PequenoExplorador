#!/usr/bin/env bash

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "${SCRIPT_DIR}/lib/common.sh"

run_unity setup-photography \
  -executeMethod PequenoExplorador.Editor.PhotographyFoundationSetup.Apply
