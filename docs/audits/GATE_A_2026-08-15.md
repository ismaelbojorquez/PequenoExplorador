# Gate A — Auditoría independiente de foundation

- Fecha: 2026-08-15 (`America/Mexico_City`).
- Auditoría sobre commit de entrada: `3503061a1d130ddaf4b23b5fbf33b3140d4651d0`.
- Rama: `main`; árbol inicial limpio; sin remoto configurado.
- Resultado: **PASS**.
- Alcance: Unity, assemblies, lifecycle, perfiles, paquetes/licencias, pipeline, Android y controles infantiles de la foundation. No es asesoría legal ni aprobación de tienda.

## Criterio de decisión

`PASS` exige import/compile, validadores, EditMode, PlayMode, dos smoke builds Android, inspección del APK y arranque `Ready` sin fatal. Un bloqueo externo se registra como `NOT RUN`/pendiente y no se convierte en evidencia. Un hallazgo estructural abierto o fallo reproducible del proyecto produce `FAIL`.

## Preflight contrastado

Se leyó completo `AGENTS.md` y la documentación de arquitectura, estándares, versiones, dependencias/licencias, políticas, testing, Android, review, riesgos, decisiones, roadmap, changelog y status. También se inspeccionaron `ProjectSettings`, `Packages`, los nueve asmdefs, todo el código runtime/Editor, tests, wrappers y workflow. Git confirmó `main`, árbol limpio, siete commits 00–06 y HEAD de entrada indicado arriba; no se aceptaron reportes históricos sin contrastar archivos/comandos.

Entorno verificado sin instalar ni actualizar:

| Componente | Evidencia Gate A |
|---|---|
| Host | macOS 26.5.2, arm64. |
| Unity | `6000.3.22f1`, revisión `1c726e1fb402`; Release API oficial: último release LTS al 2026-08-15. |
| Paquetes directos | Input System `1.20.0`, URP `17.3.0`, Test Framework `1.6.0`, uGUI `2.0.0`; pins y lock exactos, sin preview/rango dinámico. |
| Licencias de paquetes | Unity Companion License en los cuatro paquetes; aviso NVIDIA FXAA conservado por URP. No se encontró SDK comercial. |
| Android bundled | OpenJDK 17.0.18, SDK Build/Platform Tools 36.0.0, NDK r27c `27.2.12479018`, Gradle 9.1.0. |
| No disponible localmente | Unity iOS Build Support y Xcode completo; no bloquean el Gate Android-first, sí F48. |

## Matriz de hallazgos

| ID | Severidad | Hallazgo | Estado | Evidencia / resolución |
|---|---|---|---|---|
| GA-001 | Major | `Shutdown()` durante inicialización podía dejar que un servicio no cooperativo completara y devolviera el host a `Ready`; el servicio que lanzaba durante su propia inicialización no recibía cleanup. | Resuelto | `ApplicationHost` ahora posee una cancelación enlazada, queda `ShuttingDown` hasta cleanup, comprueba cancelación después de cada `await` y limpia el servicio en curso. Dos fixtures impiden regresión. |
| GA-002 | Minor | El registro 16 KB conservaba el deadline histórico 2025-11-01, distinto de la página oficial vigente. | Resuelto | Fuente oficial revalidada 2026-08-15: API 35+ requiere soporte y desde 2027-02-01 un update incompatible no podrá publicarse. Control técnico no cambia: artefacto + ejecución sin backcompat. |
| GA-003 | Info | No existe binario AAB Release firmado para inspeccionar. | Abierto aceptado | Guard de Release falla antes de `BuildPipeline` sin signing externo; símbolos persistentes vacíos, mocks quedan bajo define Development y registry Release selecciona Null/NoAds/Unavailable. AAB/firma/merged manifest Release siguen en F12/F47/F49. |
| GA-004 | Info | El emulador, con más de seis horas activo, mostró un ANR de `System UI`, dos cierres ADB al reinstalar el APK final y una activity helper de instrumentación que robó foco. | Externo, recuperado | El proceso del juego no tuvo fatal. Tras reboot no destructivo del emulador y `force-stop` del helper, el APK final se instaló, quedó top-resumed y llegó a `ApplicationReady` con backcompat 16 KB en `fatal`. Los intentos fallidos no se cuentan como PASS. |
| GA-005 | Info | CI Unity remota e iOS no son ejecutables en este entorno. | `NOT RUN` | No hay remoto/runner/licencia CI, módulo iOS ni Xcode. Pipeline local Android sí es ejecutable y es la evidencia del Gate. |
| GA-006 | Minor | El generador histórico no canónico `ProjectFoundationSetup.Apply` recrearía la escena F03 sin el `BootstrapStatusView` introducido en F06. | Deuda aceptada | No lo invoca ningún wrapper, workflow, menú ni instrucción vigente; el proyecto versionado abre/reproduce correctamente. No ejecutar ese método hasta retirarlo o actualizarlo con test en mantenimiento de foundation. |

No quedan hallazgos Critical/Major abiertos.

## Arquitectura observada

Grafo real sin ciclos:

```text
Domain (BCL)
  ↑
Application (BCL)
  ↑          ↑
Content   Infrastructure
  ↑          ↑
Presentation │
       ↑     │
       Bootstrap  ← único composition root
       ↑      ↑
Editor/Tests.EditMode   Tests.PlayMode
```

Domain no referencia `UnityEngine`; Application solo referencia Domain/BCL. Presentation no referencia Infrastructure. Solo Bootstrap conoce los concretos y su registro es tipado/interno, no un locator global. No se encontraron `FindObjectOfType`, `DontDestroyOnLoad`, singleton mutable ni listeners vivos después de shutdown. Editor y tests no entran al player.

Orden real: `MessageBus → Analytics → Ads → Purchases`; shutdown inverso. Development usa NullAnalytics/MockAds/MockPurchase solo mediante define de build; Release usa NullAnalytics/NoAds/UnavailablePurchase y oculta diagnóstico Development.

## Evidencia ejecutada

| Comando / inspección | Resultado |
|---|---|
| `scripts/validate` sobre HEAD de entrada | `PASS`, código 0, 1:13.68; repository/compile/content/fronteras, EditMode 19/19, PlayMode 2/2 y APK. |
| `scripts/build-android-development` repetido con Git limpio | `PASS`, código 0, 27.252 s; mismo tamaño, hash distinto esperado por metadata temporal del APK. |
| APK run 1 | 57,069,510 bytes; SHA-256 `96df5b6b2d314e17735667968208c8d75f2602132af36ca05b4834a9dd9b8db3`. |
| APK run 2 | 57,069,510 bytes; SHA-256 `2116d9ed050e9040607731f62041f608992899288021a794185009548598191b`. |
| `aapt2 dump badging/permissions` | min/target/compile 26/36/36; solo `arm64-v8a`; `INTERNET` y permiso interno non-exported. Sin cámara, micrófono, ubicación, contactos, storage ni `AD_ID`. |
| `zipalign -c -P 16 -v 4` + `llvm-readelf -lW` | `PASS`; siete `.so`, todos los `LOAD` con `0x4000`. |
| Instalación y cold start x2 en `sdk_gphone16k_arm64` | `PASS` para el juego; landscape, Unity `6000.3.22f1`, IL2CPP/ARM64 y evento `ApplicationReady`. El ANR externo de System UI del run 1 está en GA-004. |
| Fixtures nuevas, primer diseño síncrono | `FAIL`, dos procesos de test terminados por el auditor con código 143: el fixture bloqueaba el Unity synchronization context. Se cambió el test a `async Task`; no se atribuye al producto. |
| `scripts/test-editmode` tras corregir fixture y host | `PASS`, código 0, 20.085 s; 21/21, 0 fallos. |
| `scripts/validate` final | `PASS`, código 0, 9:11.06 tras recompilación IL2CPP completa; EditMode 21/21, PlayMode 2/2 y APK Development. |
| Verificación JUnit manual | Un intento con ruta inexistente `scripts/check-junit.rb` falló con `LoadError`; se corrigió al comando real `scripts/tools/nunit_to_junit.rb`, que reportó 21/0 y 2/0. No se cuenta el intento erróneo como prueba. |
| APK final corregido | 57,079,091 bytes; SHA-256 `8710f8ccf27489fa72ec9b9130014e0ec0f79fdadea621edf670189c582fb22f`; API 26/36, IL2CPP/ARM64. |
| APK final en emulador | `PASS`: `PAGE_SIZE=16384`, backcompat `fatal`/disabled, install, cold start top-resumed, `ApplicationReady` y diagnóstico `Ready` visible. |
| `scripts/build-android-release` | Guard esperado: código 3 en 11.523 s, antes de build/signing; no se generó artefacto Release. |
| CI GitHub, AAB firmado, iOS | `NOT RUN`, causas en GA-003/GA-005. |

Los logs de Unity incluyen mensajes de renovación de licencia/config CDN y teardown del build server de .NET; los comandos que importan, prueban y construyen terminaron con sus códigos/reportes explícitos. No se clasifican como errores de consola del proyecto.

## Seguridad infantil y permisos

- No hay backend, SDK de ads/IAP/analytics, telemetría remota, login ni secretos.
- Unity Connect/Analytics/Ads/IAP/Crash/Performance permanecen deshabilitados en ProjectSettings.
- Release es fail-closed y ad-free: analytics Null, ads NoAds, compras Unavailable.
- El permiso `INTERNET` pertenece al APK Development de Unity; R-025 exige comparar el merged manifest del AAB Release antes de tienda.
- Google Families y Apple Kids fueron revalidados en fuentes oficiales el 2026-08-15. La ausencia actual de datos/SDKs reduce superficie; no certifica cumplimiento legal ni sustituye declaraciones humanas de tienda.

## Riesgo residual y cierre

Permanecen: bundle ID/company placeholder, licencia/titularidad, AAB/firma/manifest Release, dispositivo físico, iOS, CI remota, performance, revisión legal/store y el generador histórico GA-006 fuera del flujo canónico. Ninguno se oculta como evidencia de este Gate Android foundation. Compile, suites, build, artefacto final y ejecución 16 KB cumplen; los hallazgos que afectaban runtime/política quedaron corregidos y no hay Critical/Major abierto. Veredicto: **Gate A PASS**.

---

## Revalidación independiente posterior sobre HEAD F07

- Fecha/hora de cierre: 2026-08-15 11:59 CST (`America/Mexico_City`).
- Commit de entrada: `de04fa5f5ea6171619364d76d0a882455dea1a63` (`feat(core): add additive scene flow and local addressables`).
- Rama/remoto observados: `main`, `origin/main` en el mismo commit.
- Resultado: **PASS**; cero Critical/Major abierto.
- Desviación de estado: el encargo esperaba F00–06, pero HEAD ya contenía F07. Se auditó la realidad F07 sin revertir historia ni avanzar gameplay.
- Cambio ajeno preservado: `docs/prompts/00_MASTER_CODEX_PROMPTS.md`, no versionado y excluido del diff/commit. Por ello el status global no puede declararse limpio.

### Preflight y superficie revisada

Se releyeron completos `AGENTS.md`, `STATUS`, índice, arquitectura, estándares, versiones, dependencias/licencias, políticas, testing, Android, review, decisiones, riesgos, roadmap, changelog, plan operativo y auditoría Gate A original. Se inspeccionaron `ProjectSettings`, `Packages/manifest.json`, lock, nueve asmdefs, Bootstrap/Application/Infrastructure/Presentation, scene flow/Addressables, Editor BuildTools/validadores, tests, wrappers y workflow. Git, implementación y artefactos prevalecieron sobre reportes previos.

Entorno observado sin instalación ni cambio global:

| Componente | Evidencia 2026-08-15 |
|---|---|
| Unity | `6000.3.22f1`, revisión `1c726e1fb402`; el API oficial de releases lo devuelve primero para `6000.3` y fecha `2026-08-13`. |
| Paquetes directos | Addressables `4.0.1`, Input System `1.20.0`, URP `17.3.0`, Test Framework `1.6.0`, uGUI `2.0.0`; pins/lock exactos, sin preview ni rango dinámico. |
| Licencias | Los cinco paquetes conservan `LICENSE.md` Unity Companion License; URP conserva Third Party Notices. No hay SDK comercial. |
| Android bundled | OpenJDK `17.0.18`, SDK Build/Platform Tools `36.0.0`, NDK r27c `27.2.12479018`, Gradle `9.1.0`; coinciden con la tabla Unity 6.3 salvo que la revisión exacta del JDK es la distribuida por Hub. |
| Git/tooling | Git `2.50.1`, Git LFS `3.7.1`; Android Build Support disponible. |
| No disponible | iOS Build Support y Xcode completo; iOS `NOT RUN`. |

### Fuentes oficiales temporales revalidadas

| Requisito | Fuente | Fecha de consulta | Impacto |
|---|---|---|---|
| Último parche 6.3 LTS | [Unity release API](https://services.api.unity.com/unity/editor/release/v1/releases?limit=10&stream=LTS&version=6000.3) y [release 6000.3.22f1](https://unity.com/releases/editor/whats-new/6000.3.22f1) | 2026-08-15 | Pin correcto; no migración. |
| SDK/NDK/JDK soportados | [Unity 6.3 — supported dependency versions](https://docs.unity3d.com/6000.3/Documentation/Manual/android-supported-dependency-versions.html) | 2026-08-15 | SDK tools 36.0.0, NDK r27c y JDK 17 coinciden localmente. |
| Android API | [Unity Android requirements](https://docs.unity3d.com/6000.3/Documentation/Manual/android-requirements-and-compatibility.html) y [Google Play target API](https://support.google.com/googleplay/android-developer/answer/11926878?hl=es-419) | 2026-08-15 | Unity admite API 36; desde 2026-08-31 Play exige API 36 para nuevas apps/updates. El APK usa target/compile 36. |
| Páginas 16 KB | [Android Developers](https://developer.android.com/guide/practices/page-sizes) | 2026-08-15 | APK, zip y siete ELF se inspeccionan; todos cumplen alineación 16 KB. |
| Emulador | [Unity Android requirements — emulator compatibility](https://docs.unity3d.com/6000.3/Documentation/Manual/android-requirements-and-compatibility.html#emulator-compatibility) | 2026-08-15 | Unity declara que no soporta emuladores Android; el intento AVD inconcluso no se usa como PASS/FAIL del APK. |
| Addressables | [Unity Registry](https://packages.unity.com/com.unity.addressables) y [manual 4.0](https://docs.unity3d.com/Packages/com.unity.addressables@4.0/manual/index.html) | 2026-08-15 | Registry resolvió `4.0.1`, Unity `6000.0+`; import/build/tests reales confirman compatibilidad con el pin. |

Las políticas infantiles siguen siendo controles de ingeniería y no asesoría legal. Families/Kids y declaraciones de tienda deben revalidarse por humanos antes de Release.

### Matriz de hallazgos de revalidación

| ID | Severidad | Hallazgo | Estado | Evidencia / resolución |
|---|---|---|---|---|
| GAR-001 | Minor | F07 versionó `AddressableAssetsData/link.xml` y `.meta`; Addressables los elimina/regenera y cambia el GUID durante builds. | Resuelto | Retirados del índice y añadidos a `.gitignore`. El APK conserva `assets/aa/AddressablesLink/link.xml`; no se pierde contenido runtime. |
| GAR-002 | Info | HEAD contiene F07, no solo F00–06. | Aceptado | Auditoría ampliada al estado real; no se revirtió ni añadió gameplay. |
| GAR-003 | Info | Existe un archivo ajeno no versionado bajo `docs/prompts/`. | Externo preservado | No editado ni staged; se reporta en status final. |
| GAR-004 | Info | Dos AVD API 37/16 KB presentaron ANR en System UI, teléfono y servicios Google antes de un primer frame estable. | `INCONCLUSIVE` | `PAGE_SIZE=16384`, APK instalado y proceso Unity iniciado sin fatal del juego, pero no hubo `Ready` estable. Unity no soporta emuladores; falta dispositivo físico. |
| GAR-005 | Info | APK Development es `debuggable` y conserva `INTERNET` más permiso interno non-exported. | Aceptado solo Development | No hay cámara, micrófono, ubicación, contactos, storage ni `AD_ID`; catálogo es local y sin endpoint. R-025 bloquea Release hasta inspeccionar AAB/manifest final. |
| GAR-006 | Info | AAB Release firmado, CI Unity e iOS no son ejecutables/autorizados aquí. | `NOT RUN`/`BLOCKED` | Guard Release generó reporte `PE_RELEASE_SIGNING_REQUIRED`; no existen signing, runner/licencia CI, módulo iOS ni Xcode. |

GA-006 histórico (generador F03 obsoleto) permanece deuda menor; no lo invoca el pipeline. No se abrió ninguna desviación arquitectónica nueva.

### Arquitectura y seguridad observadas

- Grafo de nueve assemblies sin ciclos; Domain/Application tienen `noEngineReferences=true`; Presentation solo referencia Application; Editor/EditMode no entran al player.
- Bootstrap sigue siendo el único composition root. No hay service locator global, singleton mutable ni `DontDestroyOnLoad` disperso; las búsquedas de objetos están limitadas a setup Editor/tests.
- `AppContext` es explícito; orden `MessageBus → Analytics → Ads → Purchases`, shutdown inverso, cancelación/cleanup y listeners cubiertos.
- Release está fail-closed: NullAnalytics, NoAds, UnavailablePurchase y diagnóstico/mocks solo bajo símbolos Editor/Development. Los símbolos persistentes Android están vacíos.
- Scene flow F07 respeta Application→Infrastructure/Presentation, owner único de handles Addressables, perfiles/grupos locales y cero endpoint remoto.
- Unity Connect, Analytics, Ads, Purchasing, Crash y Performance están deshabilitados. No hay backend, login, SDK comercial, secretos ni datos infantiles.

### Evidencia ejecutada en esta revalidación

| Comando / inspección | Resultado |
|---|---|
| `scripts/check-repository` | `PASS`, 0.50 s; Markdown/JSON/YAML, links, shell y secret scan. Incluyó el Markdown ajeno sin modificarlo. |
| `scripts/compile` baseline | `PASS`, 22.93 s; `assemblies=9 cycles=0`, contenido y Addressables local válidos. |
| `scripts/test-editmode` baseline | `PASS`, 120.48 s; `29/29`, cero fallos (28 proyecto + 1 stub del paquete). Incluye fixtures inválidas controladas. |
| `scripts/test-playmode` baseline | `PASS`, 59.23 s; `4/4`, Ready único, reload, fallo/retry y Camp↔Jungle↔Camp repetido tres veces sin leak. |
| `scripts/validate` | `PASS`, código 0, 109.43 s; checks, compile, Addressables, EditMode `29/29`, PlayMode `4/4` y APK. |
| Segundo `scripts/build-android-development` | `PASS`, código 0, 62.22 s. |
| APK run 1 | `artifacts/builds/PequenoExplorador-development-gate-a-run1.apk`; 41,722,038 bytes; SHA-256 `03df45c6f5bfaaa9e54a56027d04bd85b88a6a9d9d03214da66d05efb1fe61ae`; build interno 22,478 ms. |
| APK run 2 | `artifacts/builds/PequenoExplorador-development-gate-a-run2.apk`; 41,722,037 bytes; SHA-256 `0a9ae311635ec636e4d70057d6c4a6a8c861d60727ed4d0571fbdc20930bb1e1`; build interno 40,183 ms. |
| `aapt2 dump badging/permissions/xmltree` | `PASS`: package placeholder, min/target/compile 26/36/36, solo `arm64-v8a`; `INTERNET` + permiso interno; sin permisos sensibles/`AD_ID`. |
| ZIP/contenido local | `PASS`: `zipalign -c -P 16 -v 4`, catálogo/settings y bundles `SharedLocal`/`JungleLocal` incluidos; update-on-start deshabilitado y sin endpoint remoto. |
| `llvm-readelf -lW` | `PASS`: siete `.so`; todos los segmentos `LOAD` alineados `0x4000`. |
| `scripts/build-addressables-local` posterior a GAR-001 | `PASS`, código 0, 44.11 s; regeneró `link.xml`/`.meta` como ignorados, sin diff unstaged. |
| Release guard | Ejecutado; reporte actual `BLOCKED`, `signingMaterialLoaded=false`, razón `PE_RELEASE_SIGNING_REQUIRED`; no se produjo AAB/artefacto Release. |
| Runtime AVD cold boot/instalación | `INCONCLUSIVE`, no PASS: API 37, `PAGE_SIZE=16384`, proceso Unity/IL2CPP/ARM64 inició; ANR del sistema invitado impidió observar `Ready` estable. Captura/logcat quedaron en `artifacts/`. |
| CI Unity, AAB firmado, Android físico, iOS | `NOT RUN`; faltan autorización/infraestructura/toolchain indicados en GAR-004/GAR-006. |

Entre corridas se comparó `git status`: no apareció cache/build versionado; solo persistió el defecto `link.xml` hasta aplicar GAR-001, los documentos propios y el archivo ajeno conocido. Los hashes diferentes y un byte de diferencia son metadata temporal esperable; perfil, API, ABI, contenido y controles coinciden.

### Decisión

La foundation actual importa/compila, aplica fronteras, alcanza `Ready` y error recuperable en PlayMode, excluye mocks/debug de Release por configuración probada, ejecuta suites sin error y produce dos smoke APK Android válidos. Manifest, contenido local, ARM64/IL2CPP y readiness 16 KB pasan. El intento de emulador adicional no es evidencia favorable ni fallo reproducible del proyecto y queda pendiente en dispositivo físico.

Con GAR-001 resuelto y cero Critical/Major abierto, el veredicto de revalidación es **GATE A: PASS**. No autoriza gameplay, publicación, signing, claims legales ni cierre de riesgos Release/store.
