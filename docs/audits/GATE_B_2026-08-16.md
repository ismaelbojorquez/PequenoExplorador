# Gate B — Auditoría independiente del loop mínimo jugable

- Fecha: 2026-08-16 16:20 (`America/Mexico_City`).
- Commit de entrada: `32ebcb9337a5fa72e56356b83a0e3815f2fdfde0` (`feat(discovery): add persistent discovery progression`).
- Rama: `main`; árbol inicial limpio; `origin/main` diez commits por detrás.
- Resultado: **FAIL**.
- Alcance: gameplay, integración, UX infantil y calidad técnica del Vertical Slice. No es aprobación educativa, legal ni de tienda.

## Criterio de decisión

Gate B solo puede pasar si el recorrido `Camp → Selva → movimiento → interacción/actividad → fotografía → aprendizaje → estrellas → misión → álbum → mejora → Camp` existe, se completa sin atajos Development ni red, persiste y es comprensible con poca lectura. Un pipeline de foundation correcto no sustituye ese recorrido. Una precondición estructural ausente produce `FAIL`; una prueba que no puede ejecutarse se registra `NOT RUN`, nunca `PASS`.

## Preflight contrastado

Se leyeron completos `AGENTS.md`, `STATUS`, índice, Gate A, GDD, alcance/Vertical Slice, gameplay loop, último ExecPlan y testing. Se inspeccionaron historial y diffs Git, código runtime, tests, assemblies, configuración, contenido y símbolos de fotografía, álbum, economía, misiones, learning, Camp upgrade, personalización, UI/tutorial y journey.

La expectativa de entrada no coincide con el repositorio: Fase 29 no está implementada ni commiteada. `STATUS` y el historial sitúan el proyecto en Prompt 18, y el último ExecPlan es `p18-persistent-discovery.md`. No existe ExecPlan ni commit `feat(vertical-slice): assemble minimum exploration loop`.

## Matriz de hallazgos

| ID | Severidad | Hallazgo | Estado | Evidencia / acción requerida |
|---|---|---|---|---|
| GB-001 | Critical | La Fase 29 y el journey end-to-end no existen. | Abierto | HEAD es Prompt 18. No existen `CapturePhotoUseCase`, `AlbumQueryService`, wallet/grant/spend, learning session, Camp upgrade, tutorial ni test de journey. Implementar Prompts 19–29 en orden antes de repetir Gate B. |
| GB-002 | Major | Falta el contenido factual `Approved` requerido por el slice. | Abierto, humano | El único animal/discovery es neutral `Draft`/`PH_`; `MVP_SCOPE` mantiene al tucán candidato en `Pending factual approval`. Fuente, revisión factual, derechos y aprobación editorial son obligatorios. |
| GB-003 | Major | No pueden auditarse idempotencia ni checkpoints entre fotografía, discovery, economía, misión y mejora. | Abierto | Discovery aislado cubre first/repeat, pero los demás participantes y sus transacciones no existen. Los cinco runs del journey y corrupt-save dentro del flujo son `NOT RUN`. |
| GB-004 | Major | No existe experiencia de primera/segunda sesión ni modo no lector integrado. | Abierto | No hay tutorial, actividad, álbum, feedback educativo ni design system del slice. No es posible inferir la próxima acción completa sin botones/diagnóstico Development. |
| GB-005 | Minor | No hay dispositivo Android conectado para validar touch, safe area, Back, audio, lifecycle y UX infantil. | `NOT RUN` | `adb devices` no listó hardware. Aun con el software completo, esto impediría un `PASS` pleno de UX touch y permitiría como máximo `CONDITIONAL`. |
| GB-006 | Info | La foundation actual continúa sana y offline-first en configuración. | Verificado, no cierra Gate | `scripts/validate` pasó compile, validadores, Addressables local, EditMode 103/103, PlayMode 18/18 y APK Development; sin endpoint remoto ni permisos sensibles nuevos. |

El hallazgo GB-001 es estructural y por sí solo determina `FAIL`. No se aplicaron fixes: crear los sistemas ausentes sería nueva arquitectura/gameplay, expresamente fuera del alcance de esta auditoría.

## Arquitectura observada

El recorrido real disponible termina antes del Vertical Slice:

```text
Boot → Camp → Selva → tap-to-move → interacción contextual
     → discovery directo Draft Development → checkpoint/save → Camp
```

El recorrido requerido pero ausente es:

```text
actividad → fotografía → fact Approved → estrellas
→ misión → álbum → mejora de Camp → onboarding primera/segunda sesión
```

Los componentes existentes respetan casos de uso y puertos: Domain/Application permanecen sin `UnityEngine`, Bootstrap compone adaptadores, Addressables posee los handles y discovery usa repository/checkpoint. No se encontró un atajo nuevo entre UI y save porque la integración solicitada no existe. Esta observación no demuestra la arquitectura futura del slice.

## Evidencia ejecutada

| Comando / inspección | Resultado |
|---|---|
| `git status --short --branch`, rama, log y diffs | `PASS`: entrada limpia, `main`, HEAD Prompt 18. |
| Inventario de símbolos/tests runtime | `FAIL` de precondición: fotografía, álbum, economía, learning, Camp upgrade, tutorial y journey ausentes. |
| `scripts/validate` | `PASS`, código 0, `1:25.81`; compile, validadores, Addressables, EditMode, PlayMode y APK. |
| EditMode | `PASS`: 103/103, cero fallos. No contiene suite Gate B. |
| PlayMode | `PASS`: 18/18, cero fallos. No contiene journey end-to-end. |
| Journey automatizado completo | `NOT RUN`: test y sistemas no existen. |
| Cinco runs sin debug | `NOT RUN`: no hay flujo completo ejecutable. |
| Primera/segunda sesión, no lector, correcto/incorrecto | `NOT RUN`: tutorial/actividad/feedback ausentes. |
| Offline journey y cancelación transversal | `NOT RUN`: journey ausente; Addressables local aislado sí pasa. |
| Save corrupto dentro del journey | `NOT RUN`: no existe estado integrado del slice; recovery aislado de Save sí está cubierto. |
| ES/EN y ratios del journey | `NOT RUN`: UI del slice ausente; localización/input aislados sí pasan. |
| Android físico | `NOT RUN`: `adb devices` vacío. |
| APK Development | `PASS` técnico de foundation: 105,942,573 bytes, SHA-256 `6da8c8c58db1deaf2c75a58b4bc02a9c775789fa67ae77beb1a3e606f68e52e5`. |
| Permisos/alineación | `PASS`: `INTERNET` heredado + permiso interno non-exported; sin cámara/micrófono/ubicación/contactos/`AD_ID`; zipalign 16 KB correcto. |

No se generaron capturas de Gate B porque no existe una pantalla o journey que pueda representar el criterio. Los logs y reportes técnicos quedan en `artifacts/`; no se presentan como evidencia de jugabilidad.

## Seguridad infantil y contenido

- No se observó contenido masivo, padres reales, monetización real, SDK de ads/IAP/analytics, backend, login ni remote catalogs.
- El APK conserva `INTERNET` de Unity, aunque el contenido local y el código del producto no requieren red. Debe revisarse el manifest Release en un gate posterior.
- No existe recompensa duplicable porque todavía no existe la economía; esto es ausencia funcional, no una validación favorable.
- No puede evaluarse feedback no punitivo completo, comprensión sin lectura ni UX de segunda sesión.
- El fact del animal no está aprobado y bloquea cualquier afirmación educativa del slice.

## Decisión

**GATE B: FAIL.** La foundation compila y construye, pero no hay loop mínimo completo que auditar. No ejecutar Prompt 31. Resolver primero el bloqueo factual y completar Prompts 19–29 secuencialmente; después repetir esta auditoría con journey automatizado, cinco runs sin debug y al menos un Android físico para aspirar a `PASS` pleno.
